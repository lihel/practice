function count_same_elements(collection) {
  //在这里写入代码
	var newarray=[{key:temp,count:j}];
	temp=collection[0];
	 j=0;
	for(var i=1;i<collection.length;i++){
		if(temp===collection[i]){
			count++;
		}
		else
		{
			temp=collection[i];
			newarray.push([temp]);
			//j++;
		}
	}
	return newarray;
}